spring.application.name=api

spring.datasource.url=jdbc:h2:mem:defensedb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.h2.console.enabled=true
spring.h2.console.settings.web-allow-others=true


management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never

# UI base URL for building verification/reset links
app.ui.base-url=http://localhost:5173
app.jwt.secret=s3cr3t-k3y-f0r-d3f3ns3-m4n4g3m3nt-syst3m-2026

# Master key used to encrypt sensitive config values (e.g. SMTP password) at rest
# Must be overridden in production via APP_ENCRYPTION_KEY env var
app.encryption.master-key=${APP_ENCRYPTION_KEY:change-me-in-production-use-a-64-char-hex-key!!}

# Email Configuration (optional — SMTP disabled by default, falls back to mock)
# Set these env vars to enable real email delivery:
# spring.mail.host=${SMTP_HOST:}
# spring.mail.port=${SMTP_PORT:587}
# spring.mail.username=${SMTP_USERNAME:}
# spring.mail.password=${SMTP_PASSWORD:}
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true
